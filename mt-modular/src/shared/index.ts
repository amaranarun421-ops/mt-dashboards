/**
 * Shared barrel export for all shared UI primitives.
 *
 * Usage:
 *   import { PremiumCard, CardHeader, CardActionMenu, Sparkline, MiniBarChart, DonutChart } from '@/shared';
 */
export * from './cards';
export * from './charts';
