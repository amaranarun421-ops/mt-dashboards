/**
 * CRM feature — type definitions.
 *
 * Re-exported from data layer for ergonomic imports:
 *   import type { Contact, Deal } from '@/features/crm/types';
 */
export type {
  Touchpoint,
  FollowUp,
  CrmSignal,
  Deal,
  LeadIntent,
  HealthCell,
  Conversation,
  RenewalRisk,
  Meeting,
  Contact,
} from '../data/mock-data';
